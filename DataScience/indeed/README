postal codes taken from:
https://www.aggdata.com/node/86

this csv file needs some cleaning:
dos2unix us_postal_codes.csv

remove the last comma from each line:
sed -i 's/,//7' us_postal_codes.csv

Run the map_red_scrape.py program with:
python map_red_scrape.py -r emr --tokens tokens.cfg -c mrjob_config.conf  us_postal_codes.csv > mr_output.txt



